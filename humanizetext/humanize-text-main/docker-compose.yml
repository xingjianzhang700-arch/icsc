version: '3.8'

services:
  ai-humanizer:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./config:/app/config
    environment:
      - CONFIG_PATH=/app/config/config.toml
    restart: unless-stopped
